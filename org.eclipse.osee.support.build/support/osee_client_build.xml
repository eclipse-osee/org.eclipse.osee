<project name="Osee Client Build" default="build" basedir=".">
	<!-- since this ant file is assumed to be located at <workspace>\org.eclipse.osee.buildsupport, basedir will be the same -->

	<import file="${basedir}/osee_common_build.xml" />

	<!-- if behind a firewall set the following properties: proxyhost & proxyport -->
	<target name="build">
		<property name="releaseVersion" default="0.8.1RC" />
		<property name="topLevelElementId" default="org.eclipse.osee.master.feature" />
		<property name="filteredDependencyCheck" default="false" />
		<antcall target="PdeBuild" />
	</target>

</project>