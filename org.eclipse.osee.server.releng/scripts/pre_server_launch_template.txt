#!/bin/bash
#
# Startup script for OSEE Application Server
#
# Description: OSEE Application server
###################################################################

# Source function library.
[ -z "$JAVA_HOME" -a -x /etc/profile.d/java.sh ] && . /etc/profile.d/java.sh

OS_NAME=`uname -s`
OS_MACHINE=`uname -n`

